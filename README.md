# Set macOS kernel state during system startup

Values from the configuration file `/etc/sysctl.conf` are set using `sysctl` command.
For details see `man sysctl`.

## Install

```
sudo ./install.sh
```
